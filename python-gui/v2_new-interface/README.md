# Joular Reader

The python version of Joular Reader is a graphical user interface (GUI) for visualizing and analyzing energy consumption data generated by the Joular tool. It allows users to explore various aspects of their data, including method-level energy consumption, call tree analysis, and evolution over time.

## Requirements

- Python 3.10+
- PyQt6
- pandas
- pyqtgraph
- numpy

## Installation

1. Clone the repository


2. Install the required packages
```bash
pip install -r requirements.txt
```

3. Run the application
```bash
python gui.py
```

## Usage

### Graphical Interface

1. Click "Select Directory" to choose your Joular output directory
2. Navigate between tabs to view different aspects of the data
3. Use checkboxes to toggle between app-only and all methods/calltrees
4. Click "Show Evolution" on any method to view its consumption evolution graph

### Command Line

You can also use the library programmatically:

```python
from reader import JoularReader

# Initialize the reader with your Joular output directory
reader = JoularReader("path/to/pid_directory")

# Access methods
app_methods = reader.app_methods
all_methods = reader.all_methods

# Access call trees
app_call_trees = reader.app_call_trees
all_call_trees = reader.all_call_trees
```

## Data Format

The application expects Joular output data

```
joular_output/
├── pid1/
    ├── app/
    │   ├── total/
    │   │   ├── methods/
    │   │   │   └── *-filtered-methods-energy.csv
    │   │   └── calltrees/
    │   │       └── *-filtered-calltrees-energy.csv
    │   └── evolution/
    │       └── *-evolution.csv
    └── all/
        ├── total/
        │   ├── methods/
        │   │   └── *-all-methods-energy.csv
        │   └── calltrees/
        │       └── *-all-calltrees-energy.csv
        └── evolution/
            └── *-evolution.csv
├── pid2/
    ├── app/
    │   ├── total/
    │   │   ├── methods/
    │   │   │   └── *-filtered-methods-energy.csv
    │   │   └── calltrees/
    │   │       └── *-filtered-calltrees-energy.csv
    │   └── evolution/
    │       └── *-evolution.csv
    └── all/
        ├── total/
        │   ├── methods/
        │   │   └── *-all-methods-energy.csv
        │   └── calltrees/
        │       └── *-all-calltrees-energy.csv
        └── evolution/
            └── *-evolution.csv
```

or a PID directory :
```
pid1/
├── app/
│   ├── total/
│   │   ├── methods/
│   │   │   └── *-filtered-methods-energy.csv
│   │   └── calltrees/
│   │       └── *-filtered-calltrees-energy.csv
│   └── evolution/
│       └── *-evolution.csv
└── all/
    ├── total/
    │   ├── methods/
    │   │   └── *-all-methods-energy.csv
    │   └── calltrees/
    │       └── *-all-calltrees-energy.csv
    └── evolution/
        └── *-evolution.csv
```

